# data/ — daily network snapshots

Daily off-node snapshots produced by monad-sonar: testnet/mainnet peer counts discovered via auth-UDP peer discovery, plus mainnet geo/stake distribution and provider concentration (Tokyo vantage). One file per day under `daily/`, latest mirrored in `latest.json`.
